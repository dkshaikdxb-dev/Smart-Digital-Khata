// Canonical Fresh-Produce category name — the marker category a farmer supplier
// carries. Mirrors the backend constant in
// backend/src/controllers/distributor.controller.js (FRESH_CATEGORY). Keep the
// two in sync.
export const FRESH_CATEGORY = 'Fresh Produce';
